<div data-file-drop-item-id="{{id}}" class="thumbnail item {{#if uploaded}}uploaded{{/if}}">
    <a class="closer"><i class="pictos">*</i></a>
    <div class="img-container" style="background-image:url({{url}})"></div>
    <div class="caption">{{title}}</div>
</div>
