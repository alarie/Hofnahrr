/*global define*/
define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var GameCollection;

    GameCollection = Backbone.Collection.extend({
    });

    return GameCollection;
});